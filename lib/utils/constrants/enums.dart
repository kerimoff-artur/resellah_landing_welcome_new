enum TextSizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  pending,
  processing,
  shipped,
  delivered,
  cancelled,
}

enum PaymentMethods {
  cash,
  creditCard,
  debitCard,
  paypal,
  bankTransfer,
}
